---
AIGC:
    Label: "1"
    ContentProducer: 001191110102MACQD9K64018705
    ProduceID: 3629945596413484_0-drive/225563251218051168/提示词规则_v8.5_最终版.md
    ReservedCode1: ""
    ContentPropagator: 001191110102MACQD9K64028705
    PropagateID: 3629945596413484#1789614556103
    ReservedCode2: ""
---
# 视频提示词生成规则 v8.5

**适用范围**：AI 视频/漫剧单镜提示词出稿，及出稿后的确认与生产。
**来源**：§7、§8、§11.1、§12、§18 取自 [zenstory-ai/drama-skills](https://github.com/zenstory-ai/drama-skills)（storyboard / video-prompts / produce 三个 skill）；其余为自定。原版见同目录 `提示词规则_v7.md`。

**v8.5**：新增 §6.1 连续片段拼接与续镜（原续镜条目并入）——每段首尾帧为显式接口：相邻接口逐字段相等、切点落在静止态、动作无缝不重叠、底噪延续、真实尾帧接力、拼接抽查不过不靠剪辑补救；条款分级见 §6.1；§2.1/§3/§5/§12/§16/§21 联动；静止切点补「匀速稳态」等效承接（连续运动片段）。
**v8.4**：【主镜】与【时间轴】融合——逐拍区间一列，每拍 =「区间 +〔运镜〕+ 节拍」，区间只写一遍、运镜内嵌拍内；§2.1 十项 → 九项；§2.2/§4.4/§4.5/§9/§16/§20 同步；运镜校验从「区间串相等」改为「拍拍带〔〕标注」；相对链形态下每拍 =「连接词 +〔运镜〕+ 内容」（§4.2）。
**v8.3**：新增 §4.7 时间形态与校验分工，消除「相对时间链 vs 脚本硬校验」歧义；收敛【物理延续】职责、不与【主镜】【时间轴】重复；§16 补段数检查项；新增 §21 参数速查表。
**v8.2**：删去全篇版本演进自述，合并重复条目，修正断链，639 → 564 行，规则无删减。
**v8.1**：新增 §18 确认与生产闸门。
**v8.0**：引入 §1 规则分级；§4 用时间守恒取代三段区间相等；新增 §7 可生成性、§8 表演可见化、§12 参考图三级、§0 适用范围、§20 完整示例。

**配套**：`校验脚本.py`（校验 §16 第 2/4/5/6 项）。

---

# 第一部分 · 出稿前

---

## §0 适用范围（🔒 动笔前必须声明）

| 项 | 说明 |
|---|---|
| 目标模型 | Seedance 2.0 / 2.5、MiniMax H3、可灵、Veo…未声明时按通用路径写，**不套用相近模型语法** |
| 原生时长区间 | 单镜合法秒数下限–上限。不在区间内先改镜长，不留到生成时报错 |
| 提示词语言 | 跟随项目声明；未声明时沿用创作者当前语言，不默认英文 |
| 制作形态 | 生成模型 / 实拍。§7 只在生成形态生效 |

---

## §1 规则分级（🔑 先读）

| 级别 | 含义 | 违反后果 |
|---|---|---|
| 🔒 **结构级** | 可脚本机械核对，无例外 | 直接打回 |
| 👁 **审查级** | 需读画面/读文本判断 | 提交前逐条过 |
| 🛠 **默认做法** | 有理由时可偏离，须写理由 | 记入降级 |
| 🎨 **口味级** | 由导演/项目决定 | 不判错 |

**没有级别标注的规则 = 没有规则。**

---

## §2 字段体系

### §2.1 必做字段（🔒 缺一不可）

| 序 | 字段 | 作用 | 备注 |
|---|---|---|---|
| 1 | 【风格】 | 画风、光色、氛围基调 | 真人写实 / 日式二次元二选一，勿混用 |
| 2 | 【主镜】 | 时长 + 左右锚定 + **逐拍区间一列**：每拍 =「区间 +〔运镜〕+ 节拍」 | 区间只写一遍，运镜内嵌拍内（§4.4、§9）；守恒（§4.3）；切换点落在叙事转折；两种时间形态见 §4.7 |
| 3 | 【角色】 | 人物身份常量 | 禁写情绪、朝向、动作 |
| 4 | 【音色】 | 无台词 + 底噪 + 时间点音效 | 音效 ⊆ 主镜拍区间（§4.4） |
| 5 | 【物体逻辑】 | 关键道具数量与状态锁定 | |
| 6 | 【空间关系】 | 前后景分层、人物站位 | |
| 7 | 【物理延续】 | 首帧/末帧承接 + 跨镜续接标注 + 连续性锁面（哪些全程不变） | 动作节拍与运镜已由【主镜】逐拍承担，此处**不重列动作链**；续镜须声明拼接接口（§6.1） |
| 8 | 【硬锁】 | 本镜专属不可违背项 | 高危部位加固写这里 |
| 9 | 【负面词】 | 通用 + 本镜专属 | 合并进同一块投喂，不占字数 |

> **【主镜】一层视图（v8.4）**：动作节拍与运镜合在**同一列逐拍区间**里——每拍 =「区间 +〔运镜：运动方式+跟什么+看清什么〕+ 发生什么」。区间只写一遍，运镜不再单独成块。【物理延续】只管【主镜】没覆盖的：首帧末帧承接、跨镜续接、连续性锁面，**不重列动作链**——同一信息不写两遍，避免白白吃掉字数。

### §2.2 已废弃字段

【视觉重点】【叙事锚点】【时间轴】不再使用：触发 / 落幅 / 节拍全部并入【主镜】逐拍区间（v8.4），观感并入【风格】。
【氛围】【画面】【动作流】保留为**内部思考工具**，不进投喂层。

> ⚠️ **删除字段释放的是重复度，不是字数。** 触发与落幅搬进了【主镜】逐拍区间，区间就会变长——别当净收益花。

### §2.3 动笔前四问（🛠 每题须带本镜理由）

1. **脊柱**：起点 → 唯一动作 → 终点是什么？（§3）
2. **信息**：这一镜让观众新知道什么？说不出来就该并镜。
3. **运镜**：为什么用这个运镜？它要让观众看清什么？（§9）
4. **预算**：时长、字数、发声窗口够不够？（§4、§5、§11.1）

**奇观三行**（仅当本镜承担奇观功能）：① 观众冲着的那个瞬间；② 去掉领域术语后的动作；③ 什么碰什么（接触点）。

---

## §3 单镜脊柱：起点 → 唯一动作 → 终点（🔒）

- **起点**：现在时静态句，写清 姿态 / 重心 / 面向 / 目光落点 / 双手（哪只手持什么）/ 与行动相关物的距离方位。
  ✗「她准备站起来」（已含未来动作）　✓「她双手交叠压膝，视线落在脚边水洼」
- **唯一动作**：起点到终点的可见状态链。**交接、换手、放置、取回**须把接触者、接触方式、物件落点写在同一条链里，不许靠镜外瞬移接上。
- **终点**：与起点同一组字段的确定值。**必须落住**——预算里留一拍给收尾，不能只用一句「最终到达」宣告。多段连生成时终点同时是**拼接接口**（§6.1）。

**自检**：若这条脊柱原样复制到下一镜仍然成立，它就没写出本镜内容。字段值必须是本镜事实，不能是「已确认主体」这类占位句。

---

## §4 时间预算

### §4.1 区间格式（🔒）

`起-止s 内容`，禁用单秒点，首段起 0，末段止 = 总时长。

```
✗ 3s 对撞      ✓ 1.5-3s 对撞
```

### §4.2 何时才用显式秒段（🛠）

**默认用相对时间链**，保留表演与剪辑弹性：

```
首先〔运镜〕<起手状态>；接着〔运镜·跟什么〕<动作、方向与幅度>；
随后〔运镜〕<这个动作造成的结果>；与此同时<另一条通道>；最终〔运镜〕<结束状态>
```

相对链形态下运镜同样内嵌（v8.4）：每拍 =「连接词 +〔运镜〕+ 内容」，固定机位写〔固定〕（§9）。

只有**需要按秒对齐**时才展开成显式秒段：音画同步、对白挂拍、多镜容器。

- 并行通道用「与此同时」标出，**一条链只给一条**，落在串行链没占用的通道上——同一只手、同一道视线不能同时走两条。
- 可钉 1–2 个锚点秒数（「起手保持约半秒」），锚点相加须**明显小于**镜长；多到要铺满整镜时改写成显式秒段。

### §4.3 守恒律（🔒 脚本校验）

```
【主镜】逐拍区间的并集 = 总时长（或写明的终点秒数）
重叠段必须显式声明 overlap，按并集计算
```

**超出与不足同样违约**：

| 情形 | 后果 |
|---|---|
| **超出** | 末拍的动作、逐字对白、终点姿态一起被截断 |
| **不足** | 剩余秒数**不会变成静止画面**——执行端会用未经批准的动作/表情/运镜填满它，这些内容没有任何上游来源 |

所以「末段止 = 总时长」不够：**中间留空同样违规**。

> 为什么分开量：`0-2s` + `3-5s` 在 4 秒镜头上「合计正好 4 秒」，实际同时发生了截断与留白。

### §4.4 三轴对齐（🔒）

**声音不服从镜头的节拍。** 撞击音该落在撞击帧，不该铺满 `1.5-3s` 整段。

| 轴 | 约束 |
|---|---|
| **拍区间** | 完整覆盖，守恒（§4.3）——区间在【主镜】里只写一遍 |
| **运镜** | **内嵌每拍〔〕内**：每拍必有运镜，或写明〔固定〕+ 构图 + 看清什么（§9） |
| **音效** | 主镜拍区间的**子集**，宁少勿多；每个音效点落在它发生的那一拍**内**，不用独立秒点 |

### §4.5 校验脚本（🔒 不靠肉眼）

```python
# 1) 提取正文与音效里的 (\d+\.?\d*)-(\d+\.?\d*)s
# 2) 拍区间（【主镜】）：首=0、末=总时长、相邻止=下一起、无重叠（除声明 overlap）
# 3) 运镜：每拍带〔〕标注——运动方式+看清什么，或〔固定〕+构图+看清什么
# 4) 音效：每个区间被某个主镜拍区间包含
# 5) 字数：只计 CJK 字符，不计数字/拉丁/标点/区间标记/【】字段名，<= 600
```

配套脚本：`校验脚本.py`。

### §4.6 拍数（🛠）

10 秒 → **6–8 拍**。少于 5 拍内容笼统；多于 8 拍模型执行不过来。

### §4.7 时间形态与校验分工（🔒 动笔先选形态）

时间书写只有**两种形态**，动笔前先选定：

| 形态 | 何时用 | 守恒 / 对齐 / 包含如何落地 |
|---|---|---|
| **相对时间链**（默认） | §4.2 弹性写法：保留表演与剪辑弹性，用于草稿 / 非秒对齐镜 | ✗ 脚本不适用。改走**人工对账**（下） |
| **显式秒段** | §4.2 需按秒对齐：音画同步 / 对白挂拍 / 多镜容器 | ✓ 脚本校验（§4.5、§16 第 4/5/6 项） |

**相对链形态下，§4.1 区间格式、§4.3 守恒、§4.4 对齐、§4.5 脚本全部不适用**——不得把相对链误判成语句违规。改用**人工对账**承接 🔒 责任，四步：

1. **末帧落点**：「最终…」是否给出终点的确定性值（对齐 §3 终点「必须落住」）
2. **通道单线**：`首先→接着→随后` 是否让同一只手 / 视线同时走两条链（对齐 §4.2「一条链只给一条」）
3. **声画一拍**：并行通道的音效是否落在同拍动作上（对齐 §4.4「声音不服从镜头节拍」）
4. **锚点明显小于镜长**：钉的锚点秒数相加是否明显小于镜长（对齐 §4.2）

> 一句话：**相对链交给执行弹性，秒段交给脚本硬校验。** 相对链形态的守恒由这四步人工对账等价成立——是**等价承接，不是豁免**。

---

## §5 字数与删减

- 正向正文 **≤600 纯中文**。理想 400–520；超 650 模型开始丢节拍。
- **计数规则（🔒）**：只计 CJK 字符，不计数字、拉丁、标点、`1.2-3s` 这类区间标记、【】字段名。脚本校验。
- 负面词独立通道，不占额度。
- **超载删减顺序（🛠）**：【风格】修饰层 → 【物体逻辑】并入【硬锁】 → 【空间关系】 → 【音色】非必要层。
- **绝不删（🔒）**：脊柱（§3）、逐字对白、终点、连续性锁面、拼接接口（§6.1）。

---

## §6 角色卡、身份一致性与连续片段拼接

- 全片**逐字复用**，禁同义改写（🔒）。只含身份常量：年龄、发型发色、服装。
- 禁写情绪、朝向、动作（🔒）——那些属于 §3 脊柱。
- 服装 / 发型 / 五官靠参考帧保证，提示词层不承诺（🛠）。
### §6.1 连续片段拼接与续镜（多段连生成时逐段执行）

> 目标：**每一段都是可直拼的完整单元**——任意相邻两段首尾相接即成片，不依赖后期剪辑对齐。条款各自定级：🔒 可机械核对，👁 需读文本 / 画面判断。

| 条款 | 级别 | 规定 |
|---|---|---|
| **接力输入** | 🔒 | 下一段从上一段的**实际视频** + 该视频取的真实**尾帧**续接：视频管动作、节奏与声音连续，尾帧作下一段起始帧（§12）。两项真实输入都拿不到 → 标「**待续接，不可提交**」，保留内容草案；**不得**降级成独立文生视频，**不得**用计划尾帧、关键帧提示词或文字描述冒充接力输入 |
| **接口字段** | 👁 | 每段首帧、尾帧各写一句确定值：姿态 / 位置 / 面向 / 手持物 / 景别 / 光线方向。相邻两段接口**逐字段相等**——上段尾帧 = 下段首帧 |
| **静止切点** | 👁 | 切段点必须落在**状态确定的可复现帧**：优先动作静止态（停留 / 落定 / 收势）；连续运动片段（追逐 / 行进）允许改落**匀速稳态**——恒速直线、姿态稳定、无加减速无转向，画面状态确定即与静止态**等效承接，不是豁免**。「停留」（§7.3-3）既最好生成、也最好拼接 |
| **无缝不重叠** | 👁 | 动作链接续但不重复：上段做过的动作下段**不得重做**，两段间**不得**出现任何未声明的间隙动作 |
| **底噪延续** | 🔒 | 连续片段共用同一条底噪声明（§11.2），段间音底不得突变——声明逐字一致，可机械核对 |
| **拼接抽查** | 👁 | 每段生成后取尾帧与下段首帧比对（姿态 / 位置 / 景别 / 光线），不一致即退回重生成——**拼接靠生成对齐，不靠剪辑补救** |

---

## §7 可生成性（🔒）

**只在制作形态为「生成模型」时生效。** 演员能完成的精细动作，生成模型不一定能。

### §7.1 判据

**这个动作在普通生活影像里是否大量出现。**
二次自检：**念给一个没读过剧本的人，他能不能马上比划出来？** 需要先解释一遍才能比划的，模型多半也做不出来。

| | 写法 |
|---|---|
| 放心写 | 走进来、坐下、站起来、回头、点头、递东西、接过、抱住、推开门、把东西放在桌上、翻页、伸手去够 |
| 需要改写 | 用工具挡住另一件正在运动的东西、以厘米计的位移、单指精确操作小部件、四步以上双手编排、微表情作为承重信息 |

### §7.2 四类高风险写法

| 类型 | 例子 | 为什么难 |
|---|---|---|
| **精确拦截** | 两人同时争夺一件小物、挡住正在合上的东西、运动中截住某物 | 两个主体的时序与位置必须同时精确，最容易整镜崩掉 |
| **不可见的内部状态** | 「像是听见了什么」「神色一凛」「忽然明白过来」 | 是推断，不是画面；演员能演，模型没有可执行目标 |
| **否定式动作** | 「没有接话」「没有按下去」「不去看他」 | 「没有发生的事」无法生成，须改写成**正在发生**的替代动作 |
| **精细编排链** | 一拍里 ≥3 步手部动作，尤其左右手分工 | 4 秒 4 步 = 每步 1 秒，物理上不成立 |

### §7.3 改写方法（🛠 按顺序试，取第一个成立的）

1. **换承担者**：让物件或环境承担变化 ——「门被风带上」好过「他用脚勾住门」
2. **拆到相邻镜**：写字的过程难生成，「笔停在纸上」与「纸上多了一行字」都容易
3. **换成停留**：「精确地做某事」→「做完之后手没有拿开」。**停留是最容易生成的表演**
4. **交给声音**：动作不可见时，用已声明的声源承担

### §7.4 原则（🛠）

> **戏剧信息来自常见动作的组合与时机，不来自动作本身的精巧。**

| 同一个「他不想让对方看见」 | |
|---|---|
| ✗ 他用手掌挡住正在合上的笔记本边缘 | 精确拦截 |
| ✓ 他把笔记本合上，手放在盖子上没有移开 | 两个常见动作 + 一次停留 |

信息量不减——谁控制了这台笔记本、对方现在不能打开它，都看得见。**改的是实现方式，不是戏。**

改写**不得**改变已接受的镜头边界、起止状态与剧本事实（🔒）。若唯一可行的改写会改变戏剧结果，回去请求修订，不在提示词里硬写。

---

## §8 表演可见化（👁）

### §8.1 角色不是情绪滑块

不写「平静 30% → 紧张 70% → 震惊 100%」。从 agenda 与触发**选择外显通道**：

| 通道 | 可写内容 |
|---|---|
| 目光 | 寻找、回避、锁定、短暂失焦 |
| 呼吸 / 声音 | 吸气被截断、语速变化、音量反常降低 |
| 身体 | 重心撤后、肩线防御、手指停止或反复动作 |
| 语言 | 停顿、吞字、改称呼、打断、或刻意完整地说完 |
| 决断动作 | 收回手、让路、把物件推回、关门 |

### §8.2 规则

- **通道必须与景别匹配（🔒）**：近景写眉眼嘴、呼吸肩颈；中全景写姿态。**不写当前景别读不到的信号**——远景不靠细微眼睑变化，近景不必用夸张全身后退替代内心处理。
- **把副词换成节奏与行为（🛠）**：「愤怒地说」→ 先压低音量、吞回半句话、最后直呼对方全名（前提是符合台词与人物）。
- **每个真正承担本镜可见变化的角色才写一条（🛠）**；状态未变的旁观者不因「字段齐全」进主通道。
- 明确角色**对什么反应**，不写孤立的「她突然震惊」。触发须来自已接受的镜头/剧本/音频，或已可见的环境事实。

---

## §9 运镜（🛠）

- **优先级**：叙事转折 > 视觉冲击 > 机械运动。切换点落在叙事转折。
- **运镜词 = 运动方式 + 跟什么 + 要让观众看清什么**：

```
✗〔侧移〕
✓〔侧移·跟拍抡棍并落回车顶凹陷——看清棍头压出的凹陷〕
```

> 「跟什么」是动作宾语（模型据此不漂），「看清什么」是情绪理由（叙事转折的判据）。**两者都写，不互相替代。**

- **挂载方式（v8.4）**：运镜不再单独成块，逐拍写在【主镜】的〔〕里；相对时间链形态下写进动作句（§4.7）。
- **固定机位也要写死（🔒）**：写〔固定：构图 + 保持多久 + 看清什么〕。空着会被读成可以自由漂移。
- 运镜不得与人物动作**争夺注意力**；复杂运镜须有叙事理由。
- 不堆砌只适配：悲壮结局用环绕会破坏留白（🎨）。

---

## §10 景别—元素数匹配（🛠）

中全景容纳多角色 + 大场景；近景容纳半身；**勿在近景塞入过多元素**。

---

## §11 声音与对白

### §11.1 对白预算（🔒）

**秒段加总正确，不代表台词说得完。**

```
可用发声窗口 = 镜长 − 开口前等待 − 不能并行的动作 − 其他发声 − 末尾反应落拍
```

中文自然语速约 **4.1 个可发声字/秒**（不含标点）。

> 例：7 秒镜，开场与收尾各占 0.5 秒 → 只剩 6 秒发声；若完整台词估算需 8–10 秒（含句内停顿），写成「0–7 秒」也不成立。

处理顺序：① 简化非必要动作，让对白成为主事件 → ② 动作与对白合理重叠（须 script 允许）→ ③ 请求延长/拆镜 → ④ 要改台词则请求改剧本。

**不许**用更快语速、截断标记或静默删词让算术看起来成立（🔒）。固定每秒字数只能作为可覆盖估算，不能成为通用规则。

### §11.2 无台词镜（🔒）

写清**底声或静默**及其覆盖区间。嘴部状态由本镜表演决定，**不默认强制闭嘴**。

### §11.3 台词挂载（🛠）

有台词时把台词逐字挂在它发生的那一拍上。缺这条 → 口型对不上，或台词被当成画面内容渲染。

---

## §12 参考图与资产（🔒）

| 标记 | 含义 | 能否投产 |
|---|---|---|
| `IMG-…` | 图片**提示词条目** ID，不代表图片已生成/已提供/可用 | 否 |
| `REF-…` | 文件**此刻真的在项目里**且内容已核对 | 是 |
| `PLAN-…` | 创作者自己在生成时挂图，本项目没有文件 | 否 |

- 禁把提示词条目、还没生成的图、对话里的描述写成 `REF-…`。
- **「无」不等于文生视频授权。** 三态必须分清：`待补参考图：…` / `创作者已明确选择文生视频` / 已绑定。普通的「无」视为**未准备好**。
- 每张参考写清**用途**（身份 / 造型状态 / 地理 / 构图 / 尺度 / 效果 / 起始帧 / 结束帧 / 风格）+ **控制什么** + **不得控制什么**。
- 一张图承担两件事 → 拆成两个槽位。一镜最多一张起始帧、一张结束帧；写结束帧必须同时有起始帧。连续片段时起始帧首选上段**真实尾帧**（§6.1）。
- **起始帧不能提供尚未发生的终点**；构图 / 尺度 / 效果参考不能带入图中没有来源的人物、文字、道具、事件。
- 自动绑定不是把所有图塞进一镜：只选本镜可见、且身份 / 造型 / 地理 / 关键道具状态 / 起始构图**确实需要稳定**的图（🛠）。

---

## §13 负面词与文字

- 通用块 + 本镜专属块，**合并进同一块投喂**（🔒）。
- 禁止在正向写「不要X」后再在负面词重复（🔒）。
- **非画内字幕 / caption / 对白文字叠加默认不生成（🔒）**：正文直接声明「全镜任何时刻都不生成…」，不写抽象的「文字层为空」。
- **画内文字是独立允许集合（🔒）**：招牌、屏幕、字条逐字写明**字符、语言、承载面、位置**。全局 `no-text` 不能覆盖它。
- 被引号包起来的中文须逐字来自剧本；画内标牌文字可来自视觉设定（🔒）。

---

## §14 降级与豁免（🛠）

```
【降级】让路{规则 + 级别}，保{保留的核心}，因为{原因}；补偿：{具体补救}
【豁免】{冲突项}：{理由}
```

- **只有 🛠 / 🎨 级可降级（🔒）**。🔒 级不可降级——要么做到，要么改镜头边界。
- 每份交付**最多一次**降级。

---

## §15 参考检索（🔒 硬触发）

### §15.1 何时检索

满足任一即强制，动笔前先检索，不准凭印象硬写：

- 用户点名**风格流派**（国漫 / 日式二次元 / 赛博朋克 / 水墨…）
- 用户点名**题材**（巨物、高山、打斗、暧昧、灾难…已有大量成熟作品的题材）
- 用户要求「参考某某风格」
- 该题材 / 目标模型**首次在本项目出现**

### §15.2 检索什么（只取「表达方式」，不取「内容」）

| 要提取（可迁移） | 禁止提取（构成抄袭） |
|---|---|
| 运镜逻辑：仰角 / 一镜贯穿 / 尺度递进 | 具体分镜逐帧照搬 |
| 构图体系：三远法 / 留白 / 负空间 | 具体角色造型、招式设计 |
| 空间哲学：高远 / 无处可逃 / 俯仰自得 | 具体特效形态、法术外观 |
| 色彩体系：五行五色 / 青绿设色 / 低饱和 | 具体 IP 的配色代码 |
| 节奏哲学：留白 vs 填满 | 具体剧情、世界观设定 |

### §15.3 执行流程

1. **检索**：影评、学术论文、主创访谈优先于营销稿
2. **提炼**：归纳 3–5 条可迁移方法，**标明来源**
3. **批判**：找反面意见（如「画面太满」的批评），避免踩坑
4. **转化**：结合自身思考，设计**本镜独有的结构**
5. **说明**：简述参考了哪些表达方法 + 自己做了什么独特设计

> **说明写在投喂块之外（块前）**，不污染可直接粘贴的正文。§14 的降级声明同理。

### §15.4 原则

> 学的是「怎么说话」，不是「说什么」。
> 参考运镜与构图逻辑 → 允许；复制角色、招式、特效设计 → 禁止。

---

# 第二部分 · 出稿

---

## §16 出稿前自检清单

| # | 检查项 | 级别 | 通过标准 |
|---|---|---|---|
| 1 | 适用范围 | 🔒 | §0 四项已声明 |
| 2 | 字数 | 🔒 | 正向 ≤600 纯 CJK（脚本，按 §5 计数规则） |
| 3 | 字段完整 | 🔒 | §2.1 九项齐全，无废弃字段 |
| 4 | 区间守恒 | 🔒 | 首=0、末=总时长、相邻无缝、余量=0（脚本） |
| 5 | 拍拍带运镜 | 🔒 | 每拍〔〕内含 运动方式+看清什么，或〔固定〕+构图+看清什么（脚本） |
| 6 | 音效包含 | 🔒 | 音效区间 ⊆ 主镜拍区间（脚本） |
| 7 | 脊柱 | 👁 | 每镜 起点→唯一动作→终点；且**不能原样复制到下一镜** |
| 8 | 终点落住 | 👁 | 收尾有分配到的拍，非「最终到达」式宣告 |
| 9 | 可生成性 | 👁 | 无 §7.2 四类高风险写法，或已按 §7.3 改写 |
| 10 | 表演 | 👁 | 无情绪滑块；通道与景别匹配 |
| 11 | 对白 | 👁 | 可用窗口 ≥ 估算发声时长 |
| 12 | 参考 | 🔒 | IMG/REF/PLAN 未混用；用途/控制/不得控制齐全 |
| 13 | 投喂格式 | 🔒 | 一整块，【负面词】最后一行 |
| 14 | 交付纯度 | 🔒 | 正文不含文件名、版本号、项目路径、流程说明、QA 结论 |
| 15 | 降级 | 🛠 | 有违规项已声明，且只降 🛠/🎨 级，≤1 次 |
| 16 | 拍数（10s） | 🛠 | 6–8 拍（§4.6）；少于5笼统、多于8执行不过来 |
| 17 | 拼接接口 | 👁 | 多段连生成时：接力输入就位、相邻接口逐字段相等、切点静止、底噪同源（§6.1） |

> 第 2、4、5、6 项用**脚本**校验，不靠肉眼。
> **过完这 17 项才出稿。出稿之后的确认与生产见 §18。**

---

## §17 投喂格式

**一整块输出**，【负面词】作为最后一行合并在内，用户可直接全选粘贴。

块**内**只放交付内容：不含文件名、版本号、项目路径、流程说明、QA 结论。
§15 检索说明、§14 降级声明写在块**外**。

---

# 第三部分 · 出稿后

---

## §18 确认与生产闸门

> **刻意把确认放在生产之前。** 出稿只是把指令写好；付钱的是下一步。

### §18.1 四步硬闸门（🔒 顺序不可合并）

| 步 | 动作 | 关键约束 |
|---|---|---|
| 1 | **建 job** | 一种模态、明确数量、完整 prompt、参考文件、参数、输出路径。**一个 job 不混合模态** |
| 2 | **预览** | 展示本次准确的：数量、prompt、来源条目、参考绑定、输出路径、是否覆盖已有文件、目标 adapter。**此时不调用任何供应商** |
| 3 | **明确确认** | 创作者**在看过这份预览之后**才确认 |
| 4 | **执行** | 确认被消费一次；成功或失败后再次执行都必须**重新确认**——防失败重试意外产生第二笔费用 |

**确认绑定 job 指纹（🔒）**：job、prompt、参数、输出路径、直接输入任一变化，**旧确认立即失效**，须重新预览、重新确认。确认的对象是「即将花掉的这笔钱」，不是「这个镜头大致要做什么」。

### §18.2 什么不算确认（🔒）

以下**都不是**本次生产确认：

- 「继续」「都做完」「预算没问题」
- 上游内容已接受（剧本 / 分镜 / 提示词已经写完了）
- **之前确认过另一版**
- AI 自己推断的「用户应该会同意」

**不得代替创作者填写确认。** 只有明确同意**这一项当前任务**才算。

### §18.3 一个 job 不混合模态（🔒）

图片 / 视频 / TTS / 配乐分开建 job。大批量工作拆成**创作者能看清数量和成本边界**的小批，不为图方便把整季隐式塞进一次确认。

### §18.4 中断不等于要重跑（🔒）

> **视频任务在提交那一刻就已经计费，不是在拿到结果时。**

提交之后的一切——轮询几分钟、下载——都可能被进程被杀、断网、机器休眠打断。

1. 拿到供应商任务 ID 的**第一时间**就落盘（**早于第一次轮询**），该记录不随本次尝试一起删除
2. 中断后先**对账**：带着任务 ID 的未完成尝试报成「孤立任务」，动作是「**取回再决定，别重投**」
3. 用那个任务 ID **取回**结果
4. **只有在取回也确认确实失败之后**，才走重新确认 + 重投

> ⚠️ **看到「孤立任务」就直接重投，等于为同一个镜头付第二次钱。**

**取回不走确认闸门**，这是有意的：闸门防的是意外花钱，而取回不花钱。若取回也要重新确认，中断后最省事的路径就变成再付一次——那正好是闸门要防的事。

### §18.5 失败三路（🛠）

| 失败类型 | 处理 |
|---|---|
| **技术失败**（超时、限流、服务端错误） | 可做**有上限**的重试；重试仍须**新的明确确认** |
| **点名被拒的输入** | 先改**那一项**再投，改动写进新 job 重新预览 |
| **重复内容缺陷** | 回到对应的提示词环节改，不在生产层解决 |

**同一问题重试 ≤2 次，第 3 次必须换策略**（改景别 / 砍节拍 / 换写法），**禁止只加否定词**。

点名被拒的具体改法——**优先于「重试」**：

- **文本被拒** → 改写**那一句**，而不是原样重投
  「一拳砸在对方脸上，血顺着下巴滴」→「一拳挥空，对方侧身避开，桌上的杯子被带倒」（同 §7.3 换承担者）
- **参考图被拒** → 换一张构图与角色一致、画面本身合规的图
- **音频被拒** → 重录那句台词

> **原样重投的那次确认不产生修复，只产生一笔费用。**

### §18.6 凭据与配置边界（🔒）

- adapter 配置放在**项目外**，只含命令与超时
- 凭据由 adapter 自己从**进程环境或系统凭据存储**读取
- job 文件、确认记录、运行记录、看板**都不得保存密钥**

### §18.7 结果复核（🛠）

- 成功后回报**实际输出路径、媒体类型、运行状态**；连续片段同时执行 §6.1 拼接抽查
- **不要把「adapter 返回成功」写成质量结论**（🔒）
- 输出缺失、或文件的哈希 / 大小不再等于运行记录时 → 先复核当前字节或重新生产
- 「重复内容」只是**成本与诊断信号**，不能自动判定同文重试合理或不合理

### §18.8 生产后交还控制权（🛠）

生产结束**不自动**进入剪辑或审查；要装配成片或做质量复核时，由创作者点名开始。

---

# 附录

---

## §19 附录 · 表达方法库

> 条目增长到 40 条以上时，拆成独立 `表达方法库.md`。每条须按 §15.3-2 **标明来源**，否则 §15「别凭印象硬写」会与本节自相矛盾。

### §19.1 国风 / 玄幻巨物

- **三远法结构**：高远（仰视山巅）→ 深远（穿透纵深）→ 平远（横向铺开），是中国人观照山水的方式
- **俯仰自得**：以动破静，用游观代替定点；「以大观小」收摄山河，「以小见大」见个体渺小
- **压迫感 = 无处可逃**：不是「东西很大」，是从四面八方、视觉死角合围，不留空白
- **一镜贯穿 + 尺度递进**：空间尺度层层拉开，从近景无缝坠入宏大
- **笔墨入镜**：以国画笔法构建镜头（米氏云山泼墨 / 马远一角山水 / 工笔勾勒前景）
- **大处写意、小处写实**：远景泼墨、近景工笔
- **留白优先**：忌信息过满，用「无」承接「有」
- **五行五色**：烟墨青（远山雾林）· 松烟绿（草木）· 赭石棕（烟火）· 素白（留白），低饱和

### §19.2 通用运镜

- 仰拍推镜 15–30° 塑造压迫感
- 运镜须同时带**动作宾语**与**情绪理由**（见 §9）
- 不堆砌只适配：悲壮结局用环绕会破坏留白（🎨）

---

## §20 附录 · 完整示例

**任务**：10 秒单镜，武侠对峙，夜雨屋顶。目标模型已声明，原生时长 5–12s，中文正文。

```
【风格】真人写实，夜雨屋顶，冷青主调，侧逆光，低饱和，压抑而克制
【主镜】10s｜左右锚定：男在画面左，女在画面右
  ｜0-1.2s〔缓推〕两人隔三米对峙，雨丝斜落——看清对峙的距离
  ｜1.2-2.8s〔侧移·跟拍握柄的手〕男方右手抬起握刀柄
  ｜2.8-4.4s〔下摇·跟拍后退的脚跟〕女方后退半步，脚跟踩到瓦片
  ｜4.4-6.0s〔快速横摇至滑动的瓦片〕瓦片滑动，两人同时侧目
  ｜6.0-7.4s〔回摇〕男方松手垂臂——看清放弃的信号
  ｜7.4-8.6s〔缓推近〕女方收刀入鞘——看清收刀的手
  ｜8.6-10s〔极缓后拉〕两人各自转身，背对而立
【角色】男：三十岁，短寸黑发，深灰立领长衫，束腰；女：二十六岁，高马尾，靛蓝窄袖劲装
【音色】无台词；底噪：雨声与远处闷雷；1.2-2.8s 刀鞘轻响一声；2.8-4.4s 瓦片摩擦声
  ；4.4-6.0s 雨声骤密
【物体逻辑】单刀一把，全程不拔出；瓦片一片，仅滑动不碎裂
【空间关系】前景屋脊横穿画面下三分之一；两人背后各有半截烟囱；雨幕分层，中景虚化
【物理延续】首帧：两人双手空垂、相距三米。变更：手空垂→握刀柄→松手垂臂→收刀入鞘
【硬锁】刀始终不拔出；两人视线恒不相交（男看刀柄，女看瓦片）；十指可辨
【负面词】多指、斗鸡眼、眼球漂移、刀身反光突变、瓦片碎裂、雨变雪、背景出现第三人、
  过曝、糊脸、画内字幕
```

正向正文以 `校验脚本.py` 实测为准；区间 0→1.2→2.8→4.4→6.0→7.4→8.6→10 一列逐拍、守恒无缝，每拍带〔运镜〕标注，音效三点均在拍区间内。

**示例中不显然的几处改写：**

| 规则 | 体现 |
|---|---|
| §7.3-1 换承担者 | 「女方用手挡住即将滑落的瓦片」是精确拦截 → 改为让瓦片自己承担（`瓦片滑动`） |
| §7.3-3 换成停留 | 「男方放弃了杀意」是内部状态 → 改为 `松手垂臂` 并停住 |
| §7.2 否定式动作 | 不写「她没有拔刀」，写成「收刀入鞘」这个**正在发生**的动作 |
| §8 表演可见化 | 无「愤怒 / 犹豫」标签；侧目、后退半步、松手，全是可执行通道 |
| §9 运镜词 | 每一拍都带「跟什么」（跟拍握柄的手 / 跟拍后退的脚跟） |
| §11.2 无台词镜 | 写了底噪与覆盖区间 |

**出稿后**：建 1 个视频 job → 预览（1 条 prompt、0 张参考、1 个输出路径、目标 adapter）→ 确认 → 执行。中途中断先对账取回，**不重投**。

---

## §21 附录 · 参数速查表

> 以下阈值散落各节，集中于此。**改参数先改本表、再回填正文**，避免漏改；正文以本表为准。

| 参数 | 值 | 出处 |
|---|---|---|
| 正向正文上限 | ≤600 纯 CJK | §5 |
| 理想字数区间 | 400–520 | §5 |
| 模型丢节拍预警 | >650 | §5 |
| 中文自然语速 | ≈4.1 可发声字/秒（不含标点） | §11.1 |
| 10s 拍数 | 6–8 | §4.6 |
| 可钉锚点秒数 | 相加明显小于镜长 | §4.2 |
| 开场 / 收尾占用 | 各约 0.5s | §11.1 |
| 同一问题重试 | ≤2 次，第 3 次换策略 | §18.5 |
| 参考图每镜上限 | 起始帧 + 结束帧 各 1 | §12 |
| 表达方法库拆分线 | >40 条拆独立文件 | §19 |
| 拼接抽查 | 多段连生成时每段必查 | §6.1 |
| 降级次数 | 每份交付 ≤1 次，仅 🛠/🎨 级 | §14 / §16 |

---

> 本内容由 Coze AI 生成，请遵循相关法律法规及《人工智能生成合成内容标识办法》使用与传播。
